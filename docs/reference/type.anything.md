# Coerce to an unspecified type

Coerce to an unspecified type

## Usage

``` r
type.anything(x)
```

## Arguments

- x:

  any vector

## Value

the input (unless x is `NULL` in which case a
[`character()`](https://rdrr.io/r/base/character.html))
