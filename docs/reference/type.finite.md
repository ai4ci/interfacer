# Check for non-finite values

Any non finite values will cause failure of validation.

## Usage

``` r
type.finite(x)
```

## Arguments

- x:

  any vector that can be coerced to numeric

## Value

the input coerced to a numeric value, or an error if any non-finite
values detected
